{
  "name": "@sjunepark/darty",
  "version": "0.0.3",
  "type": "module",
  "bin": {
    "darty": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "bun build src/cli.ts --target=bun --outfile=dist/cli.js --minify --sourcemap=none",
    "prepack": "bun run build",
    "prepublishOnly": "bun run typecheck && bun test",
    "env:check": "varlock load",
    "eval:search-body:cli": "bun run evals/search-body/cli/run-eval.ts",
    "eval:search-body:agent:cli": "varlock run -- bun run evals/search-body/agent-cli/run-eval.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "test:live": "LIVE_DART_TESTS=1 bun test",
    "search": "bun run src/cli.ts search-body"
  },
  "dependencies": {
    "@effect/platform": "^0.91.0",
    "cheerio": "^1.1.0",
    "commander": "^14.0.3",
    "effect": "^3.17.0",
    "sanitize-html": "^2.17.3",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "^1.3.1",
    "@types/sanitize-html": "^2.16.1",
    "typescript": "^5.9.0",
    "varlock": "^0.9.0"
  }
}
