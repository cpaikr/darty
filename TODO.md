# TODO

Keep this as the ordered near-term queue. Expand only the active item in `PLAN.md`.

## Next Up

1. Implement the next `dsab007` mode after `contents`, likely `corp` or report-name search.
2. Defer TOC-aware section retrieval until the filing-level contract is stable.
